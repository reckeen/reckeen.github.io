# Makefile Demog

This repo contains a demonstration of Makefile functionality
Used as an example for this slide presentation:
https://reckeen.github.io/Makefile-presentation

* Makefile
   * the file itself
* diagram.d2
   * D2 diagram code showing an approximation of the DAG make would create as it figures out dependencies
* diagram.svg
   * the actual diagram
* Electric_Vehicle_Population_Data.csv
   * Raw data used in the makefile
* get_cols.py
   * script that gets just a few columns (make, model, year) from the csv file.
* locked_make_example/
   * sub-project showing a method for enforcing only one make invocation can be run at a time

